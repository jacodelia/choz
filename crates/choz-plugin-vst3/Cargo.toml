[package]
name = "choz-plugin-vst3"
version.workspace = true
edition.workspace = true
description = "VST3 plugin hosting for choz (pure-Rust COM bindings, no Steinberg SDK)."

[dependencies]
choz-ports.workspace = true
anyhow.workspace = true
libloading = "0.8"
# Pure-Rust VST3 COM ABI bindings — no SDK headers needed.
vst3 = "0.3"
